1. Animal 클래스를 만든다.
이름과 나이정보를 넣는다.
잠을 자는 기능을 넣는다.

2. Cat과 Dog를 만드는 데, Animal을 상속한다.
Cat에는 Meow라는 함수를 추가하고
Cat만의 개성을 살려서 잠을 자게 한다.
그 다음 eyeColor라는 속성을 추가한다.

3. Dog에는 bark라는 함수를 추가한다.
이거만 한다. 

4. 다형성 이용해서 각 한 마리씩 동물 만들어보기

5. Animal 2마리 Cat 3마리 Dog 2마리를 만들고
하나의 배열에서 이 것들이 갖는 모든 함수들(Sleep,Meow,bark를) 다 호출한다
